//find area of a cirlce

#include <stdio.h>
int main() {
    float radius, area;
    printf("Enter side: ");
    scanf("%f", &radius);
    printf("area of a circle: %f", 3.14 * radius * radius);

    return 0;
}