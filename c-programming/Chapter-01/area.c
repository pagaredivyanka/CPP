//find area of a square

#include <stdio.h>
int main() {
    float side, area;
    printf("Enter side: ");
    scanf("%f", &side);
    printf("area of a square: %f", side * side);


    return 0;
}